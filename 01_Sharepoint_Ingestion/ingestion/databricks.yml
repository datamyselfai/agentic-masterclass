# Declarative Automation Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "ingestion"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-7e11f943-f2a0.cloud.databricks.com

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://dbc-7e11f943-f2a0.cloud.databricks.com
      # We always use /Users/hassthomas@outlook.de for all resources to make sure we only have a single copy.
      root_path: /Users/hassthomas@outlook.de/.bundle/${bundle.name}/${bundle.target}
    run_as:
      # This runs as hassthomas@outlook.de in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: hassthomas@outlook.de
